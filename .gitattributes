# Enforce LF line endings for all text files so Prettier output is consistent across Windows and CI
* text=auto eol=lf

# Explicitly mark binary files so Git never tries to convert them
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
